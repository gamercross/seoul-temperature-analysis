# [보너스] 서울 기온 대시보드 서비스화

REPORT.md의 분석 결과를 기간/집계 단위/이동평균 윈도우를 바꿔가며 직접 탐색할 수 있는 Streamlit 대시보드입니다. (제출 방식: 로컬 실행 화면 + 실행 방법 문서 + 필터 변경 시나리오 설명)

## 실행 방법

```bash
pip install -r requirements.txt
streamlit run scripts/dashboard.py
```

브라우저에서 `http://localhost:8501`이 자동으로 열립니다.

## 구성

- **사이드바 필터**
  - 기간 선택 (날짜 범위 슬라이더)
  - 이동평균 윈도우 (7 / 14 / 30 / 60일)
  - 집계 단위 (일별 / 주별 / 월별)
- **본문**
  - 선택 기간의 평균/최고/최저기온, 변동성(표준편차) 카드
  - 선택 조건에 따라 갱신되는 시계열 그래프(원 데이터 + 이동평균)
  - 연도 × 월 평균기온 히트맵 (전체 기간 고정)

## 필터/기간 변경 시나리오 (실제 실행 후 확인한 동작)

| 시나리오 | 조작 | 결과로 확인한 변화 |
|---|---|---|
| 1. 기본 상태 | 기간 2023-01-01~2024-12-31, 이동평균 30일, 집계 일별 | 평균기온 12.5°C, 최고 35.6°C, 최저 -18.5°C, 변동성 10.55°C. REPORT.md의 전체 기간 통계와 일치 |
| 2. 집계 단위를 '월별'로 변경 | 라디오 버튼 클릭 | 그래프가 일별 노이즈 없이 뚜렷한 사인파 형태의 월평균 곡선으로 즉시 재렌더링됨 (계절성이 더 쉽게 눈에 들어옴) |
| 3. 기간을 2023-11 ~ 2024-12로 축소 | 슬라이더 좌측 핸들 드래그 | 상단 카드가 평균 11.3°C / 최저 -15.0°C로 즉시 재계산됨 — 겨울 구간이 포함되며 평균이 낮아지고 최저기온 기록이 갱신됨 |
| 4. 히트맵으로 연도 비교 | 스크롤 후 히트맵 확인 | 8~9월 행이 2024년 열에서 더 짙은 빨간색으로 표시되어, 2024년 여름이 2023년보다 다소 더 더웠음을 한눈에 확인 가능 |

시나리오 2, 3은 Claude Code 내장 브라우저로 직접 슬라이더를 드래그하고 라디오 버튼을 클릭해 실행 중인 대시보드에서 확인한 실제 동작입니다(수치는 화면에 표시된 값을 그대로 옮김).

## 알아둘 점

- `slider`로 고른 날짜 범위 밖 데이터는 카드/그래프 계산에서 완전히 제외됩니다(부분 포함 없음).
- 월별/주별 집계 시 이동평균 윈도우는 원래 일 단위 값을 해당 집계 단위로 나눈 근사치를 사용합니다(예: 30일 윈도우 → 월별 집계에서는 1개월 윈도우).
- 히트맵은 필터와 무관하게 항상 전체 기간(2023-2024) 기준으로 고정 표시됩니다 — 연도 간 비교를 위한 별도 뷰이기 때문입니다.
